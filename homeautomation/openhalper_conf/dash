{
    "actions": {
	"motion": {
	    "gpio_in": 7,
	    "parse": "lambda r: 'on' if r else 'off'",
	    "get": "http://idiotic:8081/api/item/Office_Motion/command/{value}",
	    "interval": 0.1,
	    "state": 0
	},
	"temp": {
            "exec": ["temp", "24"],
            "parse": "lambda r: float(r.split()[1])",
            "validate": "lambda r: r > 0 and r < 40",
            "interval": 60,
            "lifetime": 15,
            "get": "http://idiotic:8081/api/item/Office_Temperature/command/set?val={value}"
	},
	"hum": {
            "exec": ["temp",  "24"],
            "parse": "lambda r: float(r.split()[0])",
            "validate": "lambda r: r > 0 and r <= 100",
            "interval": 60,
            "lifetime": 15,
	    "get": "http://idiotic:8081/api/item/Office_Humidity/command/set?val={value}"
	}
    }
}
